<%= form_for(current_user.followed_follows.find_by(followed_id: @user.id), html: { method: :delete }, remote: true) do |f| %>
	<%= f.submit "フォロー解除",class: "btn btn-primary btn-sm" %>
	<%= Follow.where(followed_id: params[:followed_id]).size %>
<% end %>